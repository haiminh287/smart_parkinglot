tunnel: replace-with-your-tunnel-uuid
credentials-file: /etc/cloudflared/replace-with-your-tunnel-uuid.json

ingress:
  - hostname: api.example.com
    service: http://localhost:8088
    originRequest:
      connectTimeout: 30s
      noTLSVerify: false
  - service: http_status:404